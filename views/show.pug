html
	head
		title= "Guestbook all messages"
		link(href="http://fonts.googleapis.com/icon?family=Material+Icons", rel="stylesheet")
		link(type="text/css" rel="stylesheet" href="materialize-css/dist/css/materialize.min.css"  media="screen,projection")
		meta(name="viewport" content="width=device-width, initial-scale=1.0")
		link(rel='stylesheet', href='main.css')
	body
		div.container
			div.row
				div(class='col s12')
					h1 Guestbook
				div(class='col s12')
					h3 What everybody else wrote
					p All crap, unfortunately. See if this works

					each thing in data
						p#showTitle #{thing.title}
						p#showBody #{thing.body}
						br



					//- - for( var i = 0; i < data.length; i++ )
					//- 	// var textbody = result.rows[i]
					//- 	p data[i].title
					
					//- br

					a(href='index')
						p HOME
					a(href='form')
						p FORM






		script(type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js")
		script(type="text/javascript" src="materialize-css/dist/js/materialize.min.js")